- hosts: localhost
  gather_facts: valse
  vars:
    test1: play
  roles:
    - name: vars_scope
      vars:
        test3: roles
        test4: roles
  tasks:

    - include_tasks: roles/vars_scope/tasks/check_vars.yml

    - name: static import
      include_role:
        name: vars_scope
      vars:
        test4: import

    - include_tasks: roles/vars_scope/tasks/check_vars.yml

    - name: dynamic include
      include_role:
        name: vars_scope
      vars:
        test4: include

    - include_tasks: roles/vars_scope/tasks/check_vars.yml
